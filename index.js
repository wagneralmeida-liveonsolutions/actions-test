const test = {
  firstName: 'Wagner',
  lastName: 'Almeida',
  age: 18,
  address: '',
  number: ''
}

const ar = ['']

ar.map(item, index => item)

export default function (first, second, thirth) {
  return ''
}

const fun = () => {}
